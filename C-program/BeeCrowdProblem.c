#include <stdio.h>
#include <string.h>
#include <math.h>
#include "./headers/calculateor.h"

struct students
{
    char name[100];
    int id;
    float cgpa;
};

int main()
{
    /*--> Beginer problem set <---*/

    //=>>> 0
    // printf("Hello World!\n");

    //=>>> 1
    // int a, b, result;
    // scanf("%d%d", &a, &b);
    // result = a + b;
    // printf("X = %d\n", result);

    //=>>> 2
    // double r, a, n = 3.14159;
    // scanf("%lf", &r);
    // a = n * r * r;
    // printf("A=%.4lf\n", a);

    //=>>> 3
    // int a, b, soma;
    // scanf("%d%d", &a, &b);
    // soma = a + b;
    // printf("SOMA = %d\n", soma);

    //=>>> 4
    // int a, b, prod;
    // scanf("%d%d", &a, &b);
    // prod = a * b;
    // printf("PROD = %d\n", prod);

    //=>>> 5
    // double a, b, avg;
    // scanf("%lf%lf", &a, &b);
    // avg = (a * 3.5 + b * 7.5) / (3.5 + 7.5);
    // printf("MEDIA = %.5lf\n", avg);

    //=>>> 6
    // double a, b, c, avg;
    // scanf("%lf%lf%lf", &a, &b, &c);
    // avg = (a * 2 + b * 3 + c * 5) / (2 + 3 + 5);
    // printf("MEDIA = %.1lf\n", avg);

    //=>>> 7
    // int a, b, c, d, DIFERENCA;
    // scanf("%d%d%d%d", &a, &b, &c, &d);
    // DIFERENCA = (a * b - c * d);
    // printf("DIFERENCA = %d\n", DIFERENCA);

    //=>>> 8
    // int employeeNumber, workedHours;
    // float amount, SALARY;
    // scanf("%d%d%f", &employeeNumber, &workedHours, &amount);
    // SALARY = workedHours * amount;
    // printf("NUMBER = %d\n", employeeNumber);
    // printf("SALARY = U$ %.2f\n", SALARY);

    //=>>> 9
    // char firstName[100];
    // double fixedSalary, totalSold, total;
    // scanf("%s", firstName);
    // scanf("%lf%lf", &fixedSalary, &totalSold);
    // total = fixedSalary + (totalSold * (15.0 / 100));
    // printf("TOTAL = R$ %.2lf\n", total);

    //=>>> 10

    return 0;
}